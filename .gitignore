# Ignorer les fichiers de configuration spécifiques à l'environnement local
config.local.php

# Ignorer les dépendances PHP (si vous utilisez Composer plus tard)
/vendor/

# Ignorer les fichiers de log ou de cache générés par l'application
*.log
*.cache

# Ignorer les fichiers de session (si votre application PHP en crée)
/tmp/

# Ignorer les fichiers de base de données SQLite (si vous en utilisez)
*.sqlite
*.db

# Ignorer les fichiers d'IDE ou de système d'exploitation
.idea/          # JetBrains IDEs (IntelliJ, PhpStorm, WebStorm, etc.)
.vscode/        # VS Code
.DS_Store       # macOS
Thumbs.db       # Windows
ehthumbs.db     # Windows
*.bak           # Fichiers de sauvegarde

# Ignorer les fichiers d'upload si vous ne voulez pas les versionner (mais souvent, on veut les versionner si l'app les utilise)
# /uploads/

# Ignorer les fichiers de modules Node.js (si vous faites du frontend JS avancé)
/node_modules/

# Ignorer les fichiers de debug
*.swp
*.swo